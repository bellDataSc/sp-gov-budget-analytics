name: sp_gov_budget_analytics
version: 1.0.0
config-version: 2
profile: sp_gov_oracle

model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
data-paths: ["data"]
macro-paths: ["macros"]
snapshotshot-paths: ["snapshots"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

require-dbt-version: [">=1.5.0", "<2.0.0"]

models:
  sp_gov_budget_analytics:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts

vars:
  execution_date_filter: 2020-01-01
